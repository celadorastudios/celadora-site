{
  "name": "celadora-site",
  "version": "0.0.0",
  "private": true,
  "description": "celadora.net — static marketing site and legal pages",
  "type": "module",
  "scripts": {
    "serve": "node scripts/serve.mjs",
    "check:guardrails": "node scripts/check-guardrails.mjs",
    "check:privacy": "node scripts/check-privacy.mjs",
    "check:html": "html-validate \"*.html\"",
    "check:a11y": "pa11y-ci",
    "check": "npm run check:guardrails && npm run check:html && npm run check:privacy && npm run check:gallery && npm run check:a11y",
    "check:gallery": "node scripts/check-gallery.mjs"
  },
  "devDependencies": {
    "html-validate": "^9.7.1",
    "pa11y-ci": "^4.0.1",
    "puppeteer": "^24.10.2"
  }
}
